---
const { node } = Astro.props;
const isNumber = node.listItem === 'number';
---

{
  isNumber ? (
    <div class="p-default mt-12">
      <ol role="list" class="ol">
        <slot />
      </ol>
    </div>
  ) : (
    <ul role="list" class="ul">
      <slot />
    </ul>
  )
}
